const SignUpSocial = ({ social }) => {
    return (
        <div>
            <a>
                <img
                    src={`./img/footer-${social}.png `}
                />
            </a>
        </div>
    );
};

export default SignUpSocial;